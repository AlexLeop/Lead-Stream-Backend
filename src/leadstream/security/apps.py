from django.apps import AppConfig


class SecurityConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "leadstream.security"
    verbose_name = "Segurança e Autenticação"
